import React from "react";
import "../../Styles/roundedButton.css";

export default function RoundedButton({ contenido, contenido2 }) { //  {}   el propp
  return <button className="roundedButton">{contenido}</button>;
}
